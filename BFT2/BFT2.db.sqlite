CREATE TABLE module (
  id varchar(11),
  trial INTEGER DEFAULT 0,
  state BOOLEAN DEFAULT 1,
  t INTEGER DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE hwaddr (
  id varchar(11),
  hwaddr varchar(17)
);

